# Verification checklist — Wednesday 2026-07-08

Everything changed on Jul 6-7 that must be observed working in live before it
counts as done. Automated items arrive on their own — the check is "did the
message arrive and say the right thing." Manual items have the exact command.

## Arrives automatically (just read them)

| # | When (IST) | What should arrive | Pass looks like | Verifies |
|---|---|---|---|---|
| 1 | 08:00 | VM auto-start | VM RUNNING by 08:05 | instance schedule |
| 2 | 08:00-08:05 | Token refresh journal | `token minted and verified against fundlimit API` | hardened refresh (exec-bit immune) |
| 3 | 08:30→15:30, /15min | Token guard journal | `guard: token healthy (fundlimit 200)` all day | guard timer |
| 4 | 09:00 | Contract check | `CONFIG CONTRACT: PASS` in journal | wiring-gap detector |
| 5 | 10:00 | **Telegram ×2** | `FEATURE HEALTH BANKNIFTY/NIFTY: 47/47 live` | **VIX fix** — yesterday said 45/47 DEAD: vix_current; 47/47 = fix proven |
| 6 | 16:00 | VM auto-stop | VM TERMINATED after 16:00 | instance schedule |

If #5 still says `DEAD: vix_current` → the WS + fixed REST fallback both
failed in live; check `docker logs option_trading-ingestion_app-1 | grep -i vix`.

## Manual spot-checks (5 min, ~10:15 IST)

```bash
# a. VIX actually flowing in snapshots (expect a real number, not None)
docker exec option_trading-strategy_app-1 python3 -c "
import json,pathlib
line=pathlib.Path('/app/.run/snapshot_app/events.jsonl').read_text().splitlines()[-1]
s=json.loads(line).get('snapshot',{})
print(s.get('timestamp'), 'vix=', (s.get('vix_context') or {}).get('vix_current'))"

# b. Snapshots publishing from the OPEN (yesterday started only at 10:52)
docker logs option_trading-snapshot_app_nifty-1 2>&1 | grep 'health published' | head -3
#    -> first line should be ~09:16-09:21 IST with published>=1

# c. Both strategy containers on expected config (also ran automatically at 09:00)
sudo python3 /opt/option_trading/ops/check_config_contract.py
```

## During / end of session

| # | What | How | Pass looks like |
|---|---|---|---|
| 7 | BN trade count with 0.35 threshold | trades in JSONL / dashboard | ~0-4 trades (was: threshold passed 74% of bars; gates did all filtering) |
| 8 | First NIFTY live entry uses composite direction | trace `direction_source` | contains `composite`/rule source, NOT `direction_ml` |
| 9 | Giveback stop live (if any trade shows MFE ≥3% then fades) | exit reason in trace/JSONL | exit ~9% off peak, NOT an all-day hold to TIME_STOP |
| 10 | No trade held >1h at small loss | position log | dead-zone class extinct |

## After close

- [ ] Move verified rows out of DORMANT.md "Watching" section
- [ ] If NIFTY had a TRENDING day: pull the day into replay and compare live vs sim decisions bar-by-bar
- [ ] Delete this file once everything passes (or carry failures into an issue)
