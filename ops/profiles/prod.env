# PROD profile — current live baseline (regime_dual). Real money still gated by
# execution_app + tier; this only sets strategy behaviour.
ML_ENTRY_DIRECTION_MODE=regime_dual
ENTRY_VOL_GATE_ENABLED=1
OPPORTUNITY_GATE_ENABLED=0
