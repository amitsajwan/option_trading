# Strategy Eval UI

Deprecated. Strategy evaluation now lives in Quiet Operator at `/app?mode=eval`.

This package remains as a redirect fallback during the transition window. Do not add new evaluation UI features here.
