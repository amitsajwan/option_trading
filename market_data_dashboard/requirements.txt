fastapi==0.104.1
uvicorn[standard]==0.24.0
jinja2==3.1.2
requests==2.31.0
python-multipart==0.0.6
redis==5.0.1
python-dotenv==1.0.1
pymongo>=4.6,<5
# ML stack — MUST match strategy_app/requirements.txt exactly so the OPS sim
# reproduces live predictions. Unpinned versions drift (pandas 3.0 / sklearn 1.8
# changed the entry-model feature math and zeroed out all sim entries — 2026-06-01).
numpy>=1.26,<3
pandas>=2,<3
scikit-learn==1.7.2
joblib>=1.3,<2
lightgbm>=4,<5
xgboost==2.1.4
pyarrow
google-cloud-storage>=2.0,<3
